class Item {
  String? id, name, price, ratings;
  String? tags, sizes, colors, description, imagePath;

  Item({
    this.id,
    this.name,
    this.price,
    this.ratings,
    this.tags,
    this.sizes,
    this.colors,
    this.description,
    this.imagePath,
  });
}
